<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    Declares the app, its permissions, and entry point activity.
    We request INTERNET for potential future updates; it is NOT used now.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for the Android print framework -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CafeBillingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeBilling">

        <!-- Main (and only) Activity — Compose handles all screens internally -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
